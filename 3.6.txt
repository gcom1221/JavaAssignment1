A default constructor is a constructor with no parameters. Meaning if a constructor is missing, Java automatically provides one.
The instance variables of the object are initialized to default values:
0 for numbers, false for booleans, null for objects.